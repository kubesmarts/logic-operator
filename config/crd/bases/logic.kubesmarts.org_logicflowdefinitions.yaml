---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: logicflowdefinitions.logic.kubesmarts.org
spec:
  group: logic.kubesmarts.org
  names:
    kind: LogicFlowDefinition
    listKind: LogicFlowDefinitionList
    plural: logicflowdefinitions
    shortNames:
    - lfd
    - flowdef
    singular: logicflowdefinition
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.workflowName
      name: Workflow
      type: string
    - jsonPath: .status.workflowVersion
      name: Version
      type: string
    - jsonPath: .spec.runtimeRef.name
      name: Runtime
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: |-
          LogicFlowDefinition represents an immutable workflow version.

          Each instance contains one Open Workflow Specification 1.0.0 document
          and references the LogicFlowRuntime that executes it.
          Optionally referenced by LogicFlowService for external HTTP traffic routing.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              LogicFlowDefinitionSpec defines the desired state of LogicFlowDefinition.

              Immutable workflow version referencing a LogicFlowRuntime for execution.
            properties:
              flow:
                description: |-
                  Flow contains the full Open Workflow Specification 1.0.0 document.
                  Stored as raw JSON; parsed and validated via sdk-go during admission.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              runtimeRef:
                description: RuntimeRef points to the LogicFlowRuntime executing this
                  workflow.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
            required:
            - flow
            - runtimeRef
            type: object
          status:
            description: LogicFlowDefinitionStatus defines the observed state of LogicFlowDefinition.
            properties:
              conditions:
                description: Conditions represent detailed definition state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              configMapRef:
                description: ConfigMapRef references the materialized ConfigMap containing
                  the flow document.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              observedGeneration:
                description: ObservedGeneration tracks the last reconciled spec generation.
                format: int64
                type: integer
              workflowName:
                description: WorkflowName is the workflow identity extracted from
                  flow.document.name.
                type: string
              workflowNamespace:
                description: |-
                  WorkflowNamespace is the DSL namespace extracted from flow.document.namespace.
                  Not the Kubernetes namespace.
                type: string
              workflowVersion:
                description: WorkflowVersion is the version extracted from flow.document.version.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
